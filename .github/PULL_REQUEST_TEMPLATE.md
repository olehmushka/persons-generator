## Summary

<!-- What does this change do, and why? -->

## Test plan

<!-- How did you verify this works? `go build`/`go vet`/`go test` passing is
     the baseline, not the whole answer -- what did you actually run/check? -->

- [ ] `go build ./...`, `go vet ./...`, `go test ./...` pass
- [ ] `make lint` is clean (or only reports pre-existing issues, not new ones)
- [ ]
